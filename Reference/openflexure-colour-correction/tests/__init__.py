"""A test suite for the colour correction package.

To run the test suite:

* Install dev dependencies with `pip install -e .[dev]`
* Run the tests with `pytest`

The coverage report should and html coverage directory should automaically be generated.
"""
